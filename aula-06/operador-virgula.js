/*Operador vírgula
É usado para incluir múltiplas expressões em um lugar que requer uma única expressão.*/
// Exemplo de uso do operador vírgula no laço for.

for (let i = 0, j = 10; i < 5; i++, j--) {
    console.log(`i = ${i}, j = ${j}`);
  };
